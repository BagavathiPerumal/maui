<ResourceDictionary
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:maui="clr-namespace:Microsoft.Maui;assembly=Microsoft.Maui"
    xmlns:controlsPlatform="using:Microsoft.Maui.Controls.Platform">
    <DataTemplate x:Key="TabBarNavigationViewMenuItem">
        <controlsPlatform:TabBarNavigationViewItem
            x:Name="navViewItem"
            Content="{Binding Content}"
            Background="{Binding Background}" 
            IsEnabled="{Binding IsEnabled}"
            IsSelected="{Binding IsSelected, Mode=TwoWay}"
            MenuItemsSource="{Binding MenuItemsSource}"
            Icon="{Binding Icon}"
            />
    </DataTemplate>


</ResourceDictionary>
